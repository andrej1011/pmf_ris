{
  "build": {
    "buildCommand": "export PATH=\"/root/.local/share/mise/shims:$PATH\" && mvn -f BolnicaJPA/pom.xml clean install -DskipTests && mvn -f BolnicaWeb/pom.xml clean package -DskipTests && mkdir -p target && cp BolnicaWeb/target/*.jar target/"
  },
  "deploy": {
    "startCommand": "java -jar target/BolnicaWeb-0.0.1-SNAPSHOT.jar"
  }
}